# RBAC visual verification

The Admin staff directory renders at `/admin/staff` with the four-role administration surface, role selector, branch selector, secure sign-in identifier, and protected removal control.

The Admin POS preview renders at `/pos` with a selected branch scope and no access error. Role-specific behavior is additionally covered by server and component regression tests.
