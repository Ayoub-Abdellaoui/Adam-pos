# TiDB Interactive Workflow Audit Notes

- The active runtime is connected to external TiDB database `adam_pos`; its operational tables and critical workflow columns are present.
- The primary owner account is present, but the fresh TiDB database initially contained zero active branch records. This made global branch selection and every branch-scoped workflow appear unavailable after migration.
- The branch bootstrap helper existed but was never invoked by server startup. Startup now calls the helper idempotently, restoring the required Cosmetics, Bookstore, and Clothing branches without altering business transactions.
- The initial client/server action inventory found all active client tRPC namespaces registered in the root router. Further workflow and UI-state verification remains in progress.
- Rendered verification confirms the restored branch data unlocks the Global Administration, Product Catalog, POS, Invoice Intake, Branch Statistics, Branch Operations, Suppliers & Invoices, and Expenses Management routes. These screens render their relevant action controls and empty-state handling without runtime route failures.
- The remaining global and branch action surfaces also render correctly: Global User Management, Cash Out, Customers, Barcode Labels, branch Suppliers, Expenses, Cash Out, and branch Staff Management. Their intended add, log, edit, filter, and search controls are visible; empty operational tables represent the fresh external database rather than failed requests.
- After a cold restart, the authenticated Global Administration workspace renders the restored Add New Branch action and branch-scoped controls. The corrected Customers directory request returns successfully and presents its empty state without a validation failure.
- Branch creation validation audit: the form now keeps branch names and codes unique, permits reused branch categories, maps duplicate conflicts to the Arabic message "هذا الفرع أو الكود موجود مسبقاً.", and retains the existing layout and Create Branch action. The external TiDB database was reconciled to one unreferenced default branch per code before unique name/code indexes were restored.
- Invoice extraction upgrade audit: the restarted `/invoice` route renders normally with the existing intake layout. Parsed documents now carry per-product references and explicit anomaly decisions through a server-backed review session before a commit may proceed.
- External TiDB now contains the reference columns, review-session table, exception-decision audit table, and immutable extraction payload required by the upgraded invoice workflow. The final log scan found no new invoice-route or server-side validation failures.
- Product Catalog and Invoice Intake both render their existing action surfaces and empty-state handling after the reference synchronization update. The existing SKU/Reference control remains in the product entry workflow, while its value is now mirrored into the dedicated TiDB reference field.
- Customer Credit/Debt profile verification confirms the Overall Total Debt card, Make Payment, Add Debt, timestamped ledger, and Credit Invoices History render without a route crash. The new per-invoice action controls are implemented in the lower responsive table and protected by the assigned user’s branch role.
